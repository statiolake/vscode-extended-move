import * as vscode from "vscode";

// Global state key definitions
export const LAST_CHAR_KEY = "lastChar";
export const LAST_DIRECTION_KEY = "lastDirection";

// Direction definitions
export const enum Direction {
  Forward,
  Backward,
}
